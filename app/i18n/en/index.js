const param = require('./param.en.json');
const validation = require('./validation.en.json');
const message = require('./message.en.json');

module.exports = {
  param:param,
  validation:validation,
  message:message
}