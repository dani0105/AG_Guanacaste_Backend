const param = require('./param.es.json');
const validation = require('./validation.es.json');
const message = require('./message.es.json');

module.exports = {
  param:param,
  validation:validation,
  message:message
}