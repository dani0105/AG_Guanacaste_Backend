require('dotenv').config();
var path = require('path');

module.exports = {
  'url': `${process.env.DB_DIALECT}://${process.env.DB_USERNAME}:${process.env.DB_PASSWORD}@${process.env.DB_HOST}:${process.env.DB_PORT}/${process.env.DB_DATABASE}`,
  'migrations-path': path.resolve('app', 'migrations'),
  'models-path': path.resolve('app', 'models'),
  'seeders-path': path.resolve('app', 'seeders'),
}